pyspark.sql.functions.lcase

Returns str with all characters changed to lowercase.

Parameters
str : Column or str
        Input column or strings.

Examples


spark.range(1).select(lcase(sf.lit("Spark"))).show()
+------------+
|lcase(Spark)|
+------------+
|       spark|
+------------+
